/**
 * Theme configuration for the application.
 * Components should reference themeConfig values for consistent styling.
 */
export const themeConfig = {
  // Extend with component-specific theme values as needed
  // e.g. euler: { button: { ... }, tooltip: { ... } }
} as const;
